#pragma once

//18-21
//(a)缺少一个继承说明符：public/private等
//(b)错误，同一个基类不能出现两次
//(c)正确

//18-22
//A,B,C,X,Y，MI

//18-23
//(a)：允许
//(b)：允许
//(c)：允许
//(d)：允许

//18-24
//属于Bear类的成员不可以在进行访问，只能访问属于ZooAnimal的成员

//18-25
//a,Base1
//b,Base1
//c,Base2
//d,e,f先调用MI的析构函数，然后调用D2的析构函数，然后调用Base2的析构函数，
//接着调用D1的析构函数，最后调用Base1的析构函数。
